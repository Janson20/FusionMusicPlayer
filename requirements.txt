# ── 运行必需 ─────────────────────────────────────────────────
PySide6>=6.6,<7
PySide6-FluentUI-QML>=1.6
requests>=2.28
cryptography>=41

# ── 功能依赖 ─────────────────────────────────────────────────
mutagen>=1.45          # 读取本地音频的标签与时长
qrcode>=7.4            # 网易云扫码登录：本地渲染二维码
pillow>=9.0            # qrcode 的 PNG 后端

# ── 可选 ─────────────────────────────────────────────────────
# pypinyin>=0.49       # 中文歌单按拼音排序（缺失时退化为首字节序）
